# VinDex

VinDex is a VIN lookup tool built with ASP.NET Core (C#) for the API and Angular for the frontend. The API decodes VINs via NHTSA, stores vehicle data in PostgreSQL, and can optionally return associated recalls. The Angular app provides a friendly VIN search UI that can include recalls.
